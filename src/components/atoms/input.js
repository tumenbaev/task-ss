import styled from 'styled-components'

const Input = styled.input`
  width: 100%;
  padding: 0 10px;
  font-size: 1.125rem;
  outline: none;
  border: none;
  border-radius: 5px;

  ::placeholder {
    opacity: 0.4;
  }
`

export default Input
